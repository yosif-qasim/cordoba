# دليل نشر مكتبة قرطبة على Netlify

## المتطلبات الأساسية

- حساب على [Netlify](https://www.netlify.com/)
- حساب على [GitHub](https://github.com/) (موجود بالفعل)
- مستودع Git تم دفعه (تم الإعداد)

## خطوات النشر

### 1. دفع التغييرات إلى GitHub

```bash
# التأكد من أنك في المجلد الصحيح
cd /Users/yosifqassim/Downloads/cordoba

# دفع التغييرات إلى GitHub
git push origin main
```

### 2. الربط مع Netlify

#### الطريقة الأولى: عبر واجهة Netlify (موصى بها)

1. افتح [Netlify](https://app.netlify.com/)
2. سجّل الدخول بحسابك
3. اضغط على "Add new site" → "Import an existing project"
4. اختر "Deploy with GitHub"
5. امنح Netlify الصلاحيات للوصول إلى مستودعاتك
6. اختر مستودع `cordoba`
7. **إعدادات البناء ستُكتشف تلقائياً من `netlify.toml`**:
   - Build command: `hugo --gc --minify`
   - Publish directory: `public`
   - Hugo version: `0.152.2`
8. اضغط "Deploy site"

#### الطريقة الثانية: عبر Netlify CLI

```bash
# تثبيت Netlify CLI
npm install -g netlify-cli

# تسجيل الدخول
netlify login

# ربط المشروع بـ Netlify
netlify init

# اتبع التعليمات:
# - Create & configure a new site
# - اختر فريقك
# - اختر اسم الموقع أو اترك Netlify يختار
# - سيتم اكتشاف الإعدادات من netlify.toml تلقائياً

# النشر
netlify deploy --prod
```

### 3. إعدادات إضافية (اختيارية)

#### تخصيص النطاق

1. في لوحة تحكم Netlify، اذهب إلى "Domain settings"
2. اضغط "Add custom domain"
3. أدخل نطاقك (مثل: `cordoba.com`)
4. اتبع التعليمات لتحديث DNS

#### تفعيل HTTPS

- Netlify يوفر شهادة SSL مجانية تلقائياً من Let's Encrypt
- سيتم تفعيلها تلقائياً بعد نشر الموقع

#### متغيرات البيئة (إن لزم الأمر)

1. اذهب إلى "Site settings" → "Environment variables"
2. أضف أي متغيرات بيئة مطلوبة

## النشر التلقائي (Continuous Deployment)

بفضل الربط مع GitHub، كل تغيير يُدفع إلى فرع `main` سيؤدي تلقائياً إلى:

1. بناء الموقع على Netlify
2. معاينة التغييرات
3. النشر إذا نجح البناء

### معاينة فروع أخرى

- أي فرع آخر يُدفع إلى GitHub سينشئ **Deploy Preview**
- ستحصل على رابط مؤقت لمعاينة التغييرات
- مفيد لمراجعة المقالات قبل النشر النهائي

## الملفات المهمة للنشر

### `netlify.toml`

يحتوي على:
- إعدادات البناء
- نسخة Hugo المطلوبة
- متغيرات البيئة
- إعادات التوجيه والـ Headers الأمنية
- إعدادات التخزين المؤقت للأداء

### `.gitignore`

يمنع رفع:
- مجلد `public/` (يُبنى تلقائياً)
- ملفات الكاش
- ملفات النظام والـ IDE

## اختبار محلي قبل النشر

```bash
# بناء الموقع محلياً
hugo --gc --minify

# تشغيل خادم محلي
hugo server -D

# فتح المتصفح على
# http://localhost:1313
```

## استكشاف الأخطاء

### خطأ في البناء على Netlify

1. تحقق من سجلات البناء (Build logs) في Netlify
2. تأكد من نسخة Hugo في `netlify.toml`
3. تأكد من أن جميع الملفات المطلوبة موجودة في Git

### المحتوى لا يظهر

1. تأكد من `draft: false` في frontmatter المقالات
2. تحقق من بنية المجلدات
3. راجع سجلات البناء

### مشاكل الخطوط العربية

- تأكد من تحميل خط Rubik في `layouts/partials/head/custom.html`
- تحقق من إعدادات RTL في `assets/css/custom.css`

## الأوامر المفيدة

```bash
# معاينة محلية
hugo server -D

# بناء للإنتاج
hugo --gc --minify

# فحص الروابط المكسورة
hugo --gc --minify && htmlproofer public/

# تنظيف ملفات البناء
rm -rf public/ resources/

# حالة Git
git status

# دفع التغييرات
git add .
git commit -m "وصف التغيير"
git push origin main
```

## الدعم والمساعدة

### وثائق مفيدة

- [Hugo Documentation](https://gohugo.io/documentation/)
- [Netlify Documentation](https://docs.netlify.com/)
- [Hextra Theme Docs](https://imfing.github.io/hextra/)

### مشاكل شائعة

**مشكلة:** `Error: module "github.com/imfing/hextra" not found`
**الحل:**
```bash
hugo mod get -u
hugo mod tidy
```

**مشكلة:** الموقع يظهر فارغاً
**الحل:** تحقق من `baseURL` في `hugo.yaml` وتأكد أنه يطابق عنوان Netlify

## الأمان

الموقع مُعد بالفعل مع headers أمنية في `netlify.toml`:
- X-Frame-Options
- X-XSS-Protection
- X-Content-Type-Options
- Referrer-Policy

## الأداء

تم تفعيل:
- تصغير HTML/CSS/JS
- التخزين المؤقت للأصول الثابتة (1 سنة)
- Lazy loading للصور
- تحسين الخطوط

## الخطوات التالية بعد النشر

1. ✅ التأكد من عمل الموقع
2. ✅ فحص RTL في المتصفح
3. ✅ اختبار البطاقات والتنقل
4. ✅ فحص الروابط الداخلية
5. ✅ اختبار على أجهزة مختلفة
6. ⭐ إضافة Google Analytics (اختياري)
7. ⭐ إضافة نموذج تواصل (اختياري)
8. ⭐ إعداد RSS feed

---

**ملاحظة:** جميع الإعدادات جاهزة! ما عليك إلا دفع التغييرات إلى GitHub والربط مع Netlify.

🚀 نشر موفق!
